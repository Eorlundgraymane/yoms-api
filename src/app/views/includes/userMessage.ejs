<% if(typeof (userMessage) !='undefined' ){%>
    <div class="row">
        <div class="col-12">
            <p class="card" style="color:blue">
                <%=userMessage%>
            </p>
        </div>
    </div>
    <%} %>